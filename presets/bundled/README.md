# Bundled develop presets

Auto-generated JSON presets (MeraRAW `PartialDoc` format) imported from Lightroom `.xmp` / `.lrtemplate` packs.

Regenerate with:

```bash
npm run import-presets
```

These ship with the app and merge with user-saved presets in the presets panel.
