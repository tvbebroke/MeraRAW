{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Least-privilege grants for the main window. No fs write. Dialogs + core IPC only; image-byte I/O happens in the Rust core.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
