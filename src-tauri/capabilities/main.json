{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Production ACL for the main window (sole capability in release). Dig/selftest probes are registered only in debug via capabilities-dev + add_capability. Custom commands: permissions/default.toml (meraraw-ipc); filesystem I/O stays in Rust with path validation.",
  "windows": ["main"],
  "permissions": [
    "core:path:default",
    "core:event:default",
    "core:window:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-effects",
    "core:window:allow-set-background-color",
    "core:app:default",
    "core:image:default",
    "core:resources:default",
    "core:menu:default",
    "core:tray:default",
    "dialog:default",
    "window-state:default",
    "meraraw-ipc"
  ]
}
