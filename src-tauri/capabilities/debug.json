{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "debug-probes",
  "description": "Extra dig/selftest IPC probes. Granted only in debug builds; release relies on meraraw-ipc alone (probes also env-gated in handlers).",
  "windows": ["main"],
  "permissions": ["allow-dev-probes"]
}
